apiVersion: v1
kind: Pod
metadata:
  name: hello-world-pod
spec:
  containers:
    - name: hello-world-container
      image: yuvarajnbkr2015/hey
      ports:
        - name: web
          containerPort: 8080
          protocol: TCP
