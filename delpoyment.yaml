apiVersion: v1
kind: Pod
metadata:
  name: hello-world-pod
spec:
  containers:
    - name: hello-world-container
      image: yuvarajnbkr2015/hello-worldimage
      ports:
        - name: web
          containerPort: 8080
          protocol: TCP
